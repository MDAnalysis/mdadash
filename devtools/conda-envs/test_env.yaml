name: mdadash-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  - httpx

  # Pip-only installs
  #- pip:
  #  - codecov
